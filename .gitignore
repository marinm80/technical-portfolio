# Variables de entorno para docker compose (usar .env.example como plantilla)
.env
.env.*
!.env.example

# Mockups/fuente del logo (no son assets de la app; los assets reales
# procesados viven en frontend/public/brand/).
/logos/
